{
  "name": "wgpu-webgl-wasm",
  "version": "1.0.0",
  "description": "A WASM module that provides WebGPU-like functionality via WebGL2, using wgpu's GLES backend and Naga for WGSL→GLSL transpilation.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "build:wasm": "cargo build --target wasm32-unknown-unknown && wasm-bindgen --target web --out-dir pkg target/wasm32-unknown-unknown/debug/wgpu_webgl_wasm.wasm",
    "build:wasm:release": "cargo build --target wasm32-unknown-unknown --release && wasm-bindgen --target web --out-dir pkg target/wasm32-unknown-unknown/release/wgpu_webgl_wasm.wasm"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/frading/wgpu-webgl-wasm.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/frading/wgpu-webgl-wasm/issues"
  },
  "homepage": "https://github.com/frading/wgpu-webgl-wasm#readme",
  "devDependencies": {
    "@webgpu/types": "^0.1.66",
    "typescript": "^5.9.3",
    "vite": "^7.2.4"
  }
}
